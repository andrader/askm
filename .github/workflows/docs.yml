name: Docs

on:
  push:
    branches:
      - main
      - release
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync

      - name: Debug - Env info
        run: |
          pwd
          ls -F
          [ -f mkdocs.yml ] && echo "mkdocs.yml exists" || echo "mkdocs.yml NOT found"

      - name: Deploy Docs
        run: |
          uv run mkdocs gh-deploy --config-file mkdocs.yml --force
